# 해당 프로젝트 목표
## 1. UART 연결 구조

(일단 내부 변수로 지정하고 하드코딩)
- COM7
- 보드레이트115200bps
- 8-N-1

## 2. 데이터 파싱
UART로 들어오는 데이터는 다음과 같음. ex: `"ID: 0x0D4, DLC: 8, Data: 01 02 03 04 05 06 07 08\r\n"`
로깅을 위해 print()로 일단 다 출력시키는 걸 고려.

## 3. 추가 파싱
이중에, ID: 0x100인 데이터는 추가 파싱이 필요함.
- Data[0-1]: uint16_t(big-endian)의 전압데이터(mV)
- Data[2-3]: uint16_t(big-endian)의 전류데이터(mA)
- Data[4-5]: uint16_t(big-endian)의 PWM duty(퍼밀)
- Data[6]: 7-2비트는 예약(don't care, 아마 0), 1비트는 PWM 핀 상태, 0비트는 히스테리시스 핀 상태임 (ex: `00000010` (PWM 핀 켜짐, 히스테리시스 핀 꺼짐))
- Data[7]: 예약(아마 0)

이걸 일단 csv형태로 파싱해서 저장해야 함. 후술.

ex: `ID: 0x100, DLC: 8, Data: 3B EE 09 92 02 EE 02 00` -> `15342,2450,750,1,0`

## 4. GUI
GUI에는 다음이 표시되어 있어야 함.
### 버튼
- 처음 시작 시 버튼1 제외 전부 비활성화(또는 기능 안함)
- 버튼들은 적절히 배치되어야 함

#### 버튼1(`Handshake`)
- UART로 `"D4 1 00\n"` 전송
- response로 `"ID: 0XD4, DLC: 2, Data: 00 01\r\n"`이 오면 버튼2 활성화

#### 버튼2(`SCA Enable`)
- UART로 `"D4 2 2A 01\n"` 전송
- response로 `"ID: 0XD4 DLC: 2, Data: 2A 01\r\n"`이 오면 버튼2 비활성화, 버튼3 활성화, 버튼4 활성화

#### 버튼3(`SCA Disable`)
- UART로 `"D4 2 2A 00\n"` 전송
- response로 `"D4 2 2A 01\r\n"`이 오면 버튼1,2 제외 모든 버튼 비활성화

#### 버튼4(`Pos. S-curve`)
- UART로 `"D4 2 07 06\n"` 전송
- response로 `"ID: 0XD4, DLC: 2, Data: 07 06\r\n"`이 오면 버튼5 활성화

#### 버튼5(`Set Max Velocity`)
- UART로 `"D4 5 1F 01 CC CC CC\n"` 전송
- response로 `"ID: 0XD4, DLC: 2, Data: 1F 01\r\n"`이 오면 버튼6,8,9 활성화

#### 버튼6(`Start Recording`)
- 주기적으로(주기는 내부변수로 설정, 일단 1ms) UART로 `"100 1 1\n"` 전송
- 이에 대한 응답으로 `"ID: 0x100, DLC: 8, Data: 3B EE 09 92 02 EE 02 00"`이 올텐데, 위에서 설명한 데이터 파싱을 진행
- 버튼6 비활성화, 버튼7 활성화

#### 버튼7(`Stop Recording`)
- 주기적으로 보내던 `"100 1 1\n"` 중지
- 버튼6 활성화, 버튼7 비활성화

#### 버튼8(`Goto HIGH`)
- UART로 `"D4 5 0A 3A 00 00 00\n"` 전송 (응답 없음)

#### 버튼9(`Goto LOW`)
- UART로 `"D4 5 0A 00 00 00 00\n"` 전송 (응답 없음)


## 오실로스코프

- 버튼6을 눌렀을 때, 기존 그래프가 지워지고, 받아오는 데이터를 바탕으로 전압, 전류, PWM duty, 두 핀의 상태를 보여주는 그래프가 출력되어야 함.
- 그래프가 실시간으로 진행되는 형태의 모습
- 버튼7을 눌렀을 때, 그래프 그리기 종료.

## 데이터 저장

버튼6을 누르고, 버튼 7을 누를 때 까지 파싱된 csv줄을 logs라는 하위 디렉토리에 파일을 만들어 csv 데이터를 저장함. `logs/2025-10-20_15-12-54.csv`
